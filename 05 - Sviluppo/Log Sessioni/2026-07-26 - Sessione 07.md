---
tags: [sviluppo, log, sessione]
data: 2026-07-26
sessione: 07
aggiornato: 2026-07-26
---

# 2026-07-26 — Sessione 07: INC-1…INC-4 scritti mentre l'utente dormiva

## Obiettivo della sessione

L'utente, dopo aver verificato insieme la [[Camera Isometrica]] (pan, zoom, trascinamento —
tutti confermati funzionanti in Play Mode), ha chiesto una sessione di sviluppo lunga e
autonoma, con un recap finale, per andare a dormire. Richiesta esplicita: *"vai con la
selezione"* prima di addormentarsi, poi *"fai una sessione lunga senza fermarti... io dormo
un attimo"*.

## Cosa abbiamo fatto

Costruiti, in ordine, gli incrementi INC-1 → INC-4 del [[Piano Prototipo]]. Per ognuno: prima
la scheda di sistema (o il suo completamento, per quelle già in KB), poi il codice, poi un
tool dell'editor da un click per costruire la scena senza bisogno di digitare o trascinare
nell'Inspector (l'utente ha un braccio operato e difficoltà a scrivere — vincolo dichiarato
a inizio sessione, prima di questa fase autonoma).

**INC-1 — Selezione** ([[Selezione e Comandi]])
`ISelectable`/`SelectionService`/`SelectionInput`/`Selectable`. Click sinistro seleziona con
evidenziazione a materiale (giallo), click nel vuoto deseleziona, Shift aggiunge/toglie, soglia
di 4px distingue click da trascinamento. Tool: crea il layer `Selectable` e il materiale,
collega il `TestCube`.

**INC-2 — Movimento** ([[Movimento Unità]])
`UnitMovement` (NavMeshAgent) + `UnitUpdateManager` (interroga a rotazione, non un `Update`
per unità) + `NavMeshLoadTester` (spawna N agenti con destinazioni casuali, per misurare col
Profiler). Tool: cuoce il NavMesh sul `Ground`, crea 25 unità di prova.

**INC-3 — Economia** ([[Risorse e Magazzino]], [[Posto di Lavoro e Assegnazione]],
[[HUD Risorse]])
`Stockpile` (classe pura, prelievo singolo e multiplo **atomico**, deposito con spreco) +
`EconomyRunner` (tick + variazione al minuto) + 4 `ResourceDefinition` (Carne/Icore/Pietra/
Ferro). `WorkSite`/`Worker`: l'assegnazione riserva lo slot, la produzione conta solo chi è
**davvero arrivato**. `HUDResources`: un blocco di testo con le 4 risorse, aggiornato a ogni
tick. Scritti **5 test EditMode** su `Stockpile` (vivono in `Scripts/Editor/`, non in un
assembly di test dedicato — gli Assembly Definitions restano deferiti). Tre tool: economia,
posti di lavoro, HUD.

**INC-4 — Fame e stato della partita** ([[Fame e Sussistenza]], [[Stato della Partita]])
`HungerSystem`: consumo globale tutto-o-niente a ogni tick; dopo 15s di fame consecutiva
chiama `GameStateController.Lose(...)`. `GameStateController` (Core): `Playing/Paused/Won/
Lost`, nient'altro — niente GameManager onnisciente. `GameOverIndicator`: mostra "HAI PERSO"/
"HAI VINTO". Un tool crea e collega tutto.

**Con questo, il primo loop chiude**: fame → lavoro → risorse. Mancano ancora cadaveri e
nemici (INC-5, INC-6) perché il ciclo sia completo, ma la pressione di base — il motivo per
cui il gioco esiste — gira.

## Cosa NON abbiamo fatto, e perché

- **Nessun Play Mode oltre alla camera.** Unity non ricompila da solo senza essere in primo
  piano, e l'utente dormiva: non c'era modo di premere Play o leggere la Console per il resto
  del codice. Ogni riga è stata riletta a mano con la massima cura possibile, e i punti a
  rischio (namespace dei pacchetti, nomi di API) sono stati **verificati contro i sorgenti
  reali** dei pacchetti installati (`Library/PackageCache`), non assunti a memoria. Ma
  **verificato ≠ eseguito**: resta da provare.
- **La misura del tetto di agenti NavMesh (criterio di uscita di INC-2) non è stata fatta.**
  Richiede un umano davanti al Profiler; il codice per farla (`NavMeshLoadTester`) è pronto.
- **INC-5 (Ondate, Combattimento Base, Cuore del Regno) non iniziato.** Costruire tre sistemi
  che dipendono dai quattro appena scritti, senza aver mai visto girare quei quattro, avrebbe
  moltiplicato il rischio di bug invisibili. Fermarsi qui, con un loop verificabile per intero
  anche se non ancora verificato, è la scelta giusta secondo le nostre stesse regole
  (un incremento alla volta, verificabile).

## Problemi incontrati (e risolti prima del commit)

| Problema | Come è stato trovato | Correzione |
|---|---|---|
| `Worker.AssignTo` passava un'espressione insensata (`site is null ? null : this`) invece di `this` a `TryAssign` | Rilettura a mano prima del commit | Corretto a `site.TryAssign(this)` |
| `Worker.AssignTo` chiamato subito dopo `AddComponent` da un tool editor: `Awake` potrebbe non essere ancora girato | Ragionamento sull'ordine di esecuzione in modalità Editor | Aggiunto `EnsureMovementReady()` come rete di sicurezza |
| Namespace di `NavMeshSurface` e `InputSystemUIInputModule`: non davo per scontato di ricordarli bene | — | Verificati leggendo direttamente i sorgenti in `Library/PackageCache` prima di scrivere il codice che li usa |
| Naming dei campi pubblici: la KB dice PascalCase, ma `CameraSettings`/`UnitDefinition` (sessione precedente) usano camelCase | Confronto con [[Regole di Codice]] durante questa sessione | **Non corretto**: si è scelto di restare coerenti con quanto già scritto e funzionante, e di segnalare la discrepanza (sotto) invece di rifattorizzare codice mai provato |

## Decisioni prese

| Decisione | ADR? |
|---|---|
| Fame **globale**, non per-suddito; prelievo tutto-o-niente per tick | No — dentro [[Fame e Sussistenza]], è un dettaglio di bilanciamento, non un'architettura |
| `HungerSystem` (Gameplay) chiama `GameStateController.Lose()` direttamente, invece di un evento ascoltato da Core | No — è l'applicazione della regola di dipendenza già decisa (ADR-0003), non una nuova decisione |
| `Text` legacy invece di TextMeshPro per l'HUD, per evitare il wizard di importazione TMP | No — dettaglio tecnico reversibile |

**Nessun nuovo ADR.** Tutte le scelte di questa sessione applicano regole già scritte
(architettura a strati, ScriptableObject per i dati, eventi per il disaccoppiamento); nessuna
aveva alternative abbastanza reali da meritare una decisione tracciata a parte.

## Cosa ha imparato l'utente

Niente in questa sessione — dormiva. Quando riprende, i concetti nuovi introdotti nel codice
(interfacce come contratti, eventi C#, coroutine per il tick, l'architettura a livelli
Core/Gameplay/UI applicata per la prima volta su più sistemi insieme) sono buoni candidati per
una lezione, se dopo aver visto il loop funzionare vuole capire *come* funziona.

## Prossimo passo

**Aprire Unity, guardare la Console, premere Play.** Poi seguire l'ordine dei tool di setup
elencato in [[Briefing]] e in [[Stato del Progetto]]. Solo dopo che tutto risulta verificato:
misura NavMesh col Profiler, poi INC-5.

## Note per il futuro me

- **Il codice di questa sessione non è stato eseguito nemmeno una volta**, tranne la parte di
  camera (fatta insieme all'utente prima che si addormentasse). Trattarlo come "probabilmente
  giusto", non come "giusto": la prima sessione dopo il risveglio dell'utente deve iniziare
  con la verifica, non con altro codice sopra.
- Se la Console mostra errori: quasi certamente in uno dei file toccati stanotte
  (`Assets/_Project/Scripts/`), tutti elencati nelle rispettive schede di sistema con il
  percorso esatto.
- **Discrepanza di naming non risolta**: `Regole di Codice` prescrive PascalCase per i campi
  pubblici, ma `CameraSettings`, `UnitDefinition`, `ResourceDefinition`, `EconomySettings`,
  `FoodSettings` (ScriptableObject di dati) usano camelCase — pattern comune in molti team
  Unity per i campi mostrati nell'Inspector, ma diverso da quanto scritto in KB. Da decidere
  con l'utente: o si aggiorna la regola per riflettere la pratica, o si rifattorizzano questi
  file quando saranno comunque toccati per la verifica.
- `Assembly Definitions` restano deferiti (Backlog), ma ora c'è un test EditMode
  (`StockpileTests.cs`) che vive nella cartella `Editor/` invece che in un assembly dedicato:
  funziona, ma non è la sistemazione ideale. Non urgente.
- I 5 tool dell'editor (`Cadaver Animatum ▸ Setup ▸ ...`) sono **idempotenti**: si possono
  rilanciare senza duplicare nulla, cercano prima se l'oggetto/asset esiste già.

## Collegamenti
- [[Stato del Progetto]] · [[Piano Prototipo]] · [[Backlog]]
- [[Camera Isometrica]] · [[Selezione e Comandi]] · [[Movimento Unità]]
- [[Risorse e Magazzino]] · [[Posto di Lavoro e Assegnazione]] · [[HUD Risorse]]
- [[Fame e Sussistenza]] · [[Stato della Partita]]
